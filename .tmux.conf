# use 256 term for pretty colors
set -g default-terminal "screen-256color"

# increase scroll-back history
set -g history-limit 5000

#Vi keys and increase vim responsiveness
setw -g mode-keys   vi
set -sg escape-time 1
